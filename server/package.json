{
  "name": "gas-optimizer-api",
  "version": "1.0.0",
  "main": "api-only.js",
  "scripts": {
    "start": "node api-only.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": "20.x"
  }
}
```

Save (Ctrl+O, Enter, Ctrl+X).

**Fix 2: Update Railway start command to be simpler:**

In Railway Settings → Custom Start Command, change to:
```
cd server && npm install && npm start
